# Models Folder
**Job of this folder:**
- Database Models only.
- Contains SQLAlchemy classes that define the structure of the SQLite tables (Users, Sessions, Messages, etc.).
- Used for saving state to the hard drive.
