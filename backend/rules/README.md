# Rules Folder
**Job of this folder:**
- Holds the **`rules.md`** file, which acts as the physical database for all validated AI rules.
- Contains the Python logic that parses the `rules.md` file to inject relevant rules into the LLM prompts.
- Contains the extraction logic to turn raw user feedback into new rules.
