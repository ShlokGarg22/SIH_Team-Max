# API Folder
**Job of this folder:**
- Contains FastAPI route definitions (`routers/`).
- Exposes endpoints for the Next.js frontend to call (e.g., `/chat`, `/upload`, `/rules`).
- Handles incoming HTTP requests and passes them to the appropriate agent or service.
