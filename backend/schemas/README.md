# Schemas Folder
**Job of this folder:**
- Pydantic models for data validation.
- Defines exactly what JSON structures the API expects and what JSON structures the agents must return (e.g., `AgentRequest`, `AgentResponse`).
- Ensures all agents speak the exact same language.
